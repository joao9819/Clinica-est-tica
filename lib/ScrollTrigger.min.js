/*!
 * Offline ScrollTrigger fallback for this static template.
 * Replace with the official GSAP ScrollTrigger build when available.
 */
(function (window) {
  "use strict";
  if (window.ScrollTrigger) return;
  window.ScrollTrigger = {
    __isFallback: true,
    create: function () { return { kill: function () {} }; },
    refresh: function () {},
    batch: function () {}
  };
})(window);
